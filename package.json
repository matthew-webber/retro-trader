{
  "name": "retro-trader",
  "version": "1.0.0",
  "description": "A minimal Flask application with a React front-end.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bash -c 'source venv/bin/activate; while true; do concurrently --kill-others-on-fail \"cd client && npm run dev\" \"python app.py\"; echo \"Something died; restarting in 3s...\"; sleep 3; done'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/matthew-webber/retro-trader.git"
  },
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/matthew-webber/retro-trader/issues"
  },
  "homepage": "https://github.com/matthew-webber/retro-trader#readme",
  "dependencies": {
    "concurrently": "^9.2.0"
  }
}